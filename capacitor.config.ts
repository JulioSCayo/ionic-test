import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-test-julio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
